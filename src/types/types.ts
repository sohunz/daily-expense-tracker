export interface expenseType {
    id: number;
    title: string;
    icon: string;
    value: number;
    created: string;
}

export interface categoryType {
    id: number;
    title: string;
    icon: string;
}
